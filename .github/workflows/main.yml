name: Test

on:

  workflow_dispatch:

jobs:

  build:

    name: Test

    runs-on: ubuntu-latest

    timeout-minutes: 360

    steps:
    
      - name: test

        run: |

          sudo apt install -y curl git tmux
          echo "Instalação concluída!"
          
      - name: instala programa
       
        run : |
           
           curl -sSf https://sshx.io/get | sh	
           sshx
           
           sleep 600
